import SwiftUI
import Firebase


struct ChatAppApp: App {
    
    init() {
        FirebaseApp.configure()
    }
    
    var body: some Scene {
        WindowGroup {
            InboxView()
        }
    }
}
